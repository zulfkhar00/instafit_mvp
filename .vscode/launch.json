{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Go Backend (Exec mode)",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}",
            "envFile": "${workspaceFolder}/.env.dev",
            "preLaunchTask": "Run Uvicorn"
        }
    ]
}
